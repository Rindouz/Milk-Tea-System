# 奶茶系统三端协作问题汇总

> 审查日期：2026-05-11 ｜ 审查范围：Face（管理后台）、app（小程序）、Milk Tea System（后端）

---

## 一、严重功能问题（P0 — 影响核心业务，必须立即修复）

### 1.1 Sa-Token 认证拦截导致小程序公开页面白屏

**根因**：`config/SaTokenConfigure.java:56-59` 仅放行 `/user/doLogin` 和 `/user/wechatLogin`，其余全部拦截。

小程序首页（`pages/index/index.vue:81-97`）在 `onMounted` 中调用分类列表、商品列表、门店列表三个接口，用户未登录时全部被 401 拦截，页面白屏。

**涉及文件**：
- 后端 `SaTokenConfigure.java:56-59`
- 小程序 `pages/index/index.vue:81-97`、`pages/menu/index.vue:50-69`

**修复**：在 `addExclude` 中放行公开 GET 接口：
```
/milkteasystem/category/**
/milkteasystem/product/list
/milkteasystem/product/all
/milkteasystem/product/detail/**
/milkteasystem/product/stock/**
/milkteasystem/store
/milkteasystem/store/**
/milkteasystem/inventory/stock/**
```

---

### 1.2 添加商品时库存始终为 0，前端输入被忽略

**根因**：`ProductController.java:126` 硬编码 `initInventory(product.getProductId(), 0)`。

Face `Product.vue` 表单中有 `stock` 字段，录入库存值后提交到后端，但后端完全忽略，将库存固定初始化为 0。

**涉及文件**：
- 后端 `ProductController.java:126`
- 管理后台 `Face/src/views/Product.vue:98`

**修复**：从请求体读取库存值传入 `initInventory`。

---

### 1.3 小程序 userId 三处硬编码为 1，存在数据泄露风险

**根因**：微信登录后未正确保存 userId，三处代码用 `|| 1` 兜底或直接写死。

| 文件 | 行号 | 代码 |
|------|------|------|
| `pages/order/create.vue` | 89 | `uni.getStorageSync('userId') \|\| 1` |
| `pages/order/list.vue` | 73 | `uni.getStorageSync('userId') \|\| 1` |
| `pages/user/index.vue` | 76 | `userApi.getUserOrders(1, 100)` — 直接写死 |

用户登录信息丢失时，所有订单操作落到 userId=1 的账户。

**修复**：微信登录成功后将 userId 存入 storage；移除所有 `|| 1` 兜底；未登录时跳转登录页。

---

### 1.4 Face 订单管理页硬编码 userId=1，无法查看全部订单

**根因**：后端无管理员维度的"全部订单"查询接口，前端只能写死一个 userId。

`Face/src/views/Order.vue:169`：
```javascript
const response = await request.get('/orders/user/page/1', { params: { ... } })
```

**修复**：后端新增 `GET /orders/admin/page` 不限制 userId 的管理员分页查询接口。

---

### 1.5 `OrdersController.updateOrderStatus()` 双重执行 Bug

**根因**：`OrdersController.java:104-114` 中 `updateOrderStatus` 方法先同步调用了 `ordersService.updateOrderStatus()` 执行业务逻辑，随后又发送 MQ 消息。MQ Consumer（`OrderStatusUpdateConsumer.java`）收到消息后会再次调用同样的业务逻辑（payOrder/confirmOrder/cancelOrder/makeOrder），导致同一次状态变更被执行两遍。

虽然 Service 层有状态校验（如 payOrder 检查 status != 0 时返回 false），第二次执行会静默失败，但这是隐藏的 Bug，且 cancelOrder 的库存回滚逻辑可能重复执行。

**涉及文件**：
- `OrdersController.java:104-114`
- `OrderStatusUpdateConsumer.java:25-37`

**修复**：二选一 — 要么 Controller 只发 MQ 消息不直接调用 Service（与其他接口保持一致），要么只调 Service 不发 MQ 消息。

---

## 二、功能缺失问题（P1 — 尽快修复）

### 2.1 小程序订单列表缺少支付按钮

`pages/order/list.vue:30-32` 对待支付（status=0）订单只显示"取消订单"按钮，无"去支付"按钮。`api/index.js` 已定义 `orderApi.pay` 但从未被调用。

---

### 2.2 后端缺少"开始制作"API 端点

`OrdersServiceImpl.makeOrder()` 已完整实现（status 1→3），但 Controller 层没有暴露该端点。只能通过通用 `updateOrderStatus` 传入 status=3 间接调用。

**修复**：在 `OrdersController` 新增 `POST /orders/make/{orderNo}`。

---

### 2.3 小程序待取餐/制作中订单无确认取餐按钮

`pages/order/list.vue:30-32` 仅对待支付状态显示操作按钮。status=1（待取餐）和 status=3（制作中）的订单看不到"确认取餐"按钮，用户无法在小程序端完成取餐确认。

---

### 2.4 管理后台商品管理无图片上传功能

`Face/src/views/Product.vue:70-109` 表单无图片上传组件。商品图片只能通过数据库手动修改。

---

### 2.5 `application.properties` 中 Redis 配置使用了 YAML 语法，无法被解析

`application.properties:55-68` 混入 YAML 缩进格式：

```properties
spring:
    redis:
        host: localhost
         port: 6379
        password:
        database: 0
        timeout: 10s
        lettuce:
            pool:
                max-active: 8
```

`.properties` 文件只支持 `key=value` 格式，不支持 YAML 缩进。Spring Boot 无法解析这段配置，Redis 连接池参数不生效（会使用默认值）。

**修复**：改为 properties 格式或迁移到 `application.yml`：
```properties
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=
spring.redis.database=0
spring.redis.timeout=10s
spring.redis.lettuce.pool.max-active=8
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=0
spring.redis.lettuce.pool.max-wait=-1ms
```

---

### 2.6 微信登录为 Mock 实现

`UserController.java:158`：`String openid = "mock_openid_" + System.currentTimeMillis()`。

未调用微信 `jscode2session` API，每次登录创建新用户，同一微信用户反复产生新账号。

---

### 2.7 RocketMQ 强依赖，单点故障导致订单系统瘫痪

所有订单操作通过 RocketMQ 异步处理（Controller 仅发消息，Consumer 执行业务）。若 RocketMQ 不可用，整个订单系统不可用。

**涉及**：`OrdersController.java`（全部接口）、`OrderMessageProducer.java`、4 个 Consumer 类。

**修复**：增加同步降级方案，MQ 不可用时直接调用 Service。

---

## 三、数据一致性与接口对齐问题（P2）

### 3.1 Face 订单详情引用了不存在的 `completeTime` 字段

`Face/src/views/Order.vue:94`：
```html
<el-descriptions-item label="完成时间">{{ orderDetail.completeTime }}</el-descriptions-item>
```

后端 `Orders.java` 实体中没有 `completeTime` 字段，`OrderDetailDTO` 也没有。该字段始终为空。

**修复**：在 `Orders` 实体和 `OrderDetailDTO` 中增加 `completeTime` 字段，或在确认取餐时设置该时间。

---

### 3.2 订单状态码定义顺序与实际流程不一致

`entity/Orders.java:55` 注释：`0待支付 1待取餐 3制作中 2已完成 4已取消`。

状态码 2 和 3 的数字顺序与业务流转顺序相反（正常应为 2=制作中 → 3=已完成）。

---

### 3.3 用户分页接口返回密文手机号

`UserController.page()` 直接返回 `User` 实体分页结果，phone 为 AES 密文，管理后台看到的是乱码。而 `getAll()` 正确返回 `UserDTO`（已解密），两个接口行为不一致。

**涉及**：`UserController.java:106-115`

---

### 3.4 管理后台用户编辑缺少 avatar 字段

`Face/src/views/User.vue:56-62` 用户表单只有 `nickname` 和 `phone`，无法管理头像。

---

## 四、管理后台其他问题（P3）

### 4.1 库存管理页 N+1 查询

`Face/src/views/Inventory.vue:92-122` 先获取全部商品，再逐个调接口查库存和已售数量。后端已有 `/inventory/page` 分页接口和 `/inventory/all` 全量接口但前端未使用。

---

### 4.2 订单页"取消"和"确认取餐"同在 status=1 时显示，跳过制作中环节

`Order.vue:53` 和 `:61` 都在 `status===1`（待取餐）时显示。确认取餐从 status=1 直接跳到 status=2（已完成），跳过了制作中（status=3）环节。

---

## 五、小程序端其他问题（P3）

### 5.1 首页加载失败静默处理

`pages/index/index.vue:94-96` catch 块为空，数据加载失败时用户看到空白页但不清楚原因。

---

### 5.2 加入购物车后自动跳转打断浏览

`pages/product/detail.vue:72` 加入购物车后 1.2 秒自动 `switchTab` 到购物车页，用户可能想继续浏览。

---

## 六、图像/静态资源问题（仅记录，不影响功能运行）

| 编号 | 问题 | 位置 |
|------|------|------|
| 6.1 | 管理员头像 `https://example.com/admin-avatar.png` 不存在 | `UserServiceImpl.java:132` |
| 6.2 | 轮播图用本地 static 文件，无法后台动态配置 | `pages/index/index.vue:72` |
| 6.3 | 商品图片依赖 DB `image` 字段，若存相对路径则小程序无法加载 | 全部 `product.image` |
| 6.4 | tabBar 图标全部复用 `logo.png`，无差异化 | `pages.json:41-67` |

---

## 七、已修复项确认（来自 2026-05-10 报告）

| 原问题 | 状态 |
|--------|------|
| `@EnableScheduling` 缺失 | 已修复 — `MilkTeaSystemApplication.java:11` |
| `CosConfig.java` 类名冲突 | 已修复 — 文件已删除（git status 确认） |
| Sa-Token 认证被注释 | 已修复 — 拦截逻辑已恢复（`SaTokenConfigure.java:54-59`） |
| Face Order.vue 状态映射错误 | 已修复 — 状态映射与后端一致 |

---

## 八、修复优先级总览

| 优先级 | 编号 | 问题 | 影响 |
|--------|------|------|------|
| **P0** | 1.1 | 认证拦截公开接口 | 小程序首页白屏 |
| **P0** | 1.2 | 添加商品库存初始化为 0 | 库存数据错误 |
| **P0** | 1.3 | 小程序 userId 三处硬编码 | 订单归属错乱、数据泄露 |
| **P0** | 1.4 | Face 订单页硬编码 userId | 管理员看不到全部订单 |
| **P0** | 1.5 | updateOrderStatus 双重执行 | 业务逻辑被执行两遍 |
| **P1** | 2.1 | 小程序无支付按钮 | 用户无法支付 |
| **P1** | 2.2 | 缺少开始制作 API | 订单流程断裂 |
| **P1** | 2.3 | 小程序无确认取餐按钮 | 用户无法完成订单 |
| **P1** | 2.4 | 商品无图片上传 | 管理不便 |
| **P1** | 2.5 | Redis 配置 YAML 格式错误 | 连接池参数不生效 |
| **P1** | 2.6 | 微信登录 Mock | 无法用于生产 |
| **P1** | 2.7 | RocketMQ 强依赖 | 单点故障 |
| **P2** | 3.1 | completeTime 字段缺失 | 订单详情显示空值 |
| **P2** | 3.2 | 状态码顺序不合理 | 维护成本 |
| **P2** | 3.3 | 分页接口返回密文手机号 | 管理后台显示异常 |
| **P3** | 4.1 | 库存页 N+1 查询 | 性能隐患 |
| **P3** | 5.1-5.2 | 小程序交互细节 | 用户体验 |
| 记录 | 6.1-6.4 | 图像/静态资源问题 | 不影响功能 |

---

## 九、后端 API 参考

| 功能 | 方法 | 路径 | 当前认证 |
|------|------|------|----------|
| 管理员登录 | GET | `/user/doLogin` | 放行 |
| 微信登录 | POST | `/user/wechatLogin` | 放行 |
| 分类列表 | GET | `/category/list` | **被拦截** |
| 商品列表 | GET | `/product/list` | **被拦截** |
| 全部商品 | GET | `/product/all` | **被拦截** |
| 商品详情 | GET | `/product/detail/{id}` | **被拦截** |
| 商品库存 | GET | `/product/stock/{id}` | **被拦截** |
| 库存查询 | GET | `/inventory/stock/{id}` | **被拦截** |
| 门店列表 | GET | `/store` | **被拦截** |
| 门店详情 | GET | `/store/{id}` | **被拦截** |
| 门店在制数 | GET | `/store/{id}/orders/Waiting` | **被拦截** |
| 创建订单 | POST | `/orders/create` | 拦截（正确） |
| 支付订单 | POST | `/orders/pay/{orderNo}` | 拦截（正确） |
| 取消订单 | POST | `/orders/cancel/{orderNo}` | 拦截（正确） |
| 确认取餐 | POST | `/orders/confirm/{orderNo}` | 拦截（正确） |
| 用户订单 | GET | `/orders/user/{userId}` | 拦截（正确） |
| 订单详情 | GET | `/orders/detail/{orderNo}` | 拦截（正确） |
| 更新状态 | POST | `/orders/updateStatus/{orderNo}` | 拦截（正确） |

> 所有接口前缀为 `/milkteasystem`。**被拦截** = 小程序未登录时调用返回 401。

---

## 十、启动信息

**后端**（端口 8080）：
```bash
cd "Milk Tea System" && ./mvnw spring-boot:run
```
依赖：MySQL (milktea, root/root)、Redis (localhost:6379)、RocketMQ (localhost:9876)

**管理后台**（端口 3000）：
```bash
cd Face && npm install && npm run dev
```
登录：admin / admin123（`UserServiceImpl.java:127` 硬编码）

**小程序**：
```bash
cd "app/Milk Tea System" && npm install
```
用 HBuilderX 打开运行到微信开发者工具。BASE_URL 配置在 `utils/request.js:1`。
